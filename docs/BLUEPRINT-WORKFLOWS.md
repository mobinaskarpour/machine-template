# Blueprint workflows

Workflows are state-machine specifications only (not executable in Phase 3).

Each workflow includes stages, states, transitions, responsible roles, notifications, and audit flags.

Sensitive departments (finance, quality, procurement, food safety) default to approval + audit requirements.

Agents never auto-approve transitions.
