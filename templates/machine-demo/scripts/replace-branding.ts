/**
 * replace-branding.ts — stub
 * Applies branding/ + config/company.json + config/theme.json into the app.
 */
console.log("[replace-branding] Stub — sync branding/ into public/ and theme tokens");
